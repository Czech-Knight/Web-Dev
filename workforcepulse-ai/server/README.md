# WorkforcePulse AI API

TypeScript Express backend for labour market analytics, demand forecasting, skill gap ranking, and scenario simulation.

## Run

```bash
npm install
cp .env.example .env
npm run dev
```

API runs on `http://localhost:4000`.
