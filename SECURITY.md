# Security policy

Report vulnerabilities privately through GitHub's Security tab if private vulnerability reporting is enabled. Otherwise coordinate a private report with the maintainers, without posting credentials, personal information or exploit details publicly.

This monorepo contains multiple applications with separate deployment requirements; review configuration and runtime permissions for the affected app independently. The supported development branch is `yash`.
